1. find
find -name "core.[0-9]*"

2. du
du -h /meda_home/ --max_depth=1 2>/dev/null | sort -n -r
  -h: -human, could also be -BG, shown as GB unit
  -r: reverse
 
3. rename
for var in *.log; do
  mv $var "before_"$var;
done
